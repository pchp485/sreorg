"""Revenue engine: the parts of a small business that can run without a human.

Deliberately stdlib-only. It has to run on a free GitHub Actions runner with no
`pip install` step, because the whole point is that the machine costs nothing to
keep alive while it is still earning nothing.
"""

__version__ = "0.1.0"
